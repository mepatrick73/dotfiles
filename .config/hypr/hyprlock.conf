general {
    hide_cursor = true
    grace = 0
}

background {
    monitor =
    path = ~/.config/hypr/lockscreen.gif
    blur_passes = 0
}

input-field {
    monitor =
    size = 280, 48
    outline_thickness = 1
    dots_size = 0.28
    dots_spacing = 0.18
    outer_color = rgba(255, 255, 255, 0.08)
    inner_color = rgba(8, 8, 10, 0.85)
    font_color = rgb(192, 200, 208)
    fade_on_empty = true
    placeholder_text = <span foreground="##606878">password</span>
    hide_input = false
    rounding = 8
    position = 0, -80
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%H:%M')"
    color = rgba(192, 200, 208, 0.9)
    font_size = 72
    font_family = Noto Sans Mono
    position = 0, 120
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%A, %B %d')"
    color = rgba(96, 104, 120, 0.9)
    font_size = 18
    font_family = Noto Sans Mono
    position = 0, 40
    halign = center
    valign = center
}
