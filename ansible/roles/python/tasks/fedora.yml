---
- name: Install Python and pip
  dnf:
    name:
      - python3
      - python3-pip
    state: present
  become: true
  tags: [python]

- name: Install Python packages
  pip:
    name:
      - numpy
      - matplotlib
    extra_args: --user
  tags: [python]
